"""
Django AppConfig for the `levels` app.
"""

from django.apps import AppConfig


class LevelsConfig(AppConfig):
    """
    Configuration for the `levels` app in the Django project.
    """

    default_auto_field = "django.db.models.BigAutoField"
    name = "kns.levels"
