"""
Django AppConfig for the `discipleships` app.
"""

from django.apps import AppConfig


class DiscipleshipsConfig(AppConfig):
    """
    Configuration for the `discipleships` app in the Django project.
    """

    default_auto_field = "django.db.models.BigAutoField"
    name = "kns.discipleships"
