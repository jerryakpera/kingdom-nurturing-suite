"""
Django AppConfig for the custom_user app.
"""

from django.apps import AppConfig


class CustomUserConfig(AppConfig):
    """
    Configuration for the `custom_user` app in the Django project.
    """

    name = "kns.custom_user"
    verbose_name = "Custom User Management"
