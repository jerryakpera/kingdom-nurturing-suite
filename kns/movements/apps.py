"""
Django AppConfig for the `movements` app.
"""

from django.apps import AppConfig


class MovementsConfig(AppConfig):
    """
    Configuration for the `movements` app in the Django project.
    """

    default_auto_field = "django.db.models.BigAutoField"
    name = "kns.movements"
