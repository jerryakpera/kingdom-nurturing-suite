"""
Django AppConfig for the `onboarding` app.
"""

from django.apps import AppConfig


class OnboardingConfig(AppConfig):
    """
    Configuration for the `onboarding` app in the Django project.
    """

    default_auto_field = "django.db.models.BigAutoField"
    name = "kns.onboarding"
