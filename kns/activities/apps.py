"""
Django AppConfig for the `activities` app.
"""

from django.apps import AppConfig


class ActivitiesConfig(AppConfig):
    """
    Configuration for the `activities` app in the Django project.
    """

    default_auto_field = "django.db.models.BigAutoField"
    name = "kns.activities"
