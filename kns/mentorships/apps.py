"""
Django AppConfig for the `mentorships` app.
"""

from django.apps import AppConfig


class MentorshipsConfig(AppConfig):
    """
    Configuration for the `mentorships` app in the Django project.
    """

    default_auto_field = "django.db.models.BigAutoField"
    name = "kns.mentorships"
