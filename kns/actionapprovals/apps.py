"""
Django AppConfig for the `actionapprovals` app.
"""

from django.apps import AppConfig


class ActionapprovalsConfig(AppConfig):
    """
    Configuration for the `actionapprovals` app in the Django project.
    """

    default_auto_field = "django.db.models.BigAutoField"
    name = "kns.actionapprovals"
