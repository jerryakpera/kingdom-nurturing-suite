"""
Django AppConfig for the `classifications` app.
"""

from django.apps import AppConfig


class ClassificationsConfig(AppConfig):
    """
    Configuration for the `classifications` app in the Django project.
    """

    default_auto_field = "django.db.models.BigAutoField"
    name = "kns.classifications"
